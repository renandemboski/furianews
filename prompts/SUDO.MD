> **`sudo apt update`**  
> Updates the system's package list, allowing Ubuntu to know the newest available versions before installing anything.

> **`sudo apt install postgresql-client`**  
> Installs the PostgreSQL client tools, including `psql`, allowing you to connect to and manage PostgreSQL databases without installing the database server itself.
